# API

Lightweight Node HTTP API. `npm run api:start` exposes health, Scan Now, opportunities, events, and paper-trade endpoints on port 3001. It returns explicit empty/data-unavailable states until providers are configured.
