# Scanner

`npm run scan:once` invokes the shared `ScanEngine`. `npm run scanner:start` runs the application-level Asia/Kolkata weekday session scheduler approximately once per minute. No real order execution exists.
